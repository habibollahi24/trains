// import { Schema_FormType } from '@/types/formSchema';
// import { ControllerRenderProps } from 'react-hook-form';
// import Select from 'react-select/creatable';

// type Props<T> = {
//   field: ControllerRenderProps<Schema_FormType, T>;
//   options?: { value: string; label: string }[];
// };

// function MyMultiInput<T>({ field, options }: Props<T>) {
//   return <Select isMulti {...field} options={options} />;
// }

// export default MyMultiInput;
